# IndexNow response codes

The automation treats HTTP 200 (accepted) and HTTP 202 (accepted, key validation pending) as successful. Other responses fail the workflow so the submission can be reviewed.
