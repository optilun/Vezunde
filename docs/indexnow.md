# IndexNow

VIASEE hosts the IndexNow key at the site root and submits every URL from `public/sitemap.xml` through `.github/workflows/indexnow.yml` whenever the sitemap or submission script changes. The workflow can also be started manually from GitHub Actions.
