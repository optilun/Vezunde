name: PR266 Patch Probe

on:
  pull_request:
    paths:
      - scripts/apply-pr266-post-evaluation-patch.mjs
      - scripts/repair-pr266-post-evaluation-applicator.mjs
      - .github/workflows/pr266-patch-probe.yml

concurrency:
  group: pr266-patch-probe-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  probe:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/patient-conversation-agent-contract
          fetch-depth: 1

      - name: Repair patch applicator target list
        shell: cmd
        run: node scripts/repair-pr266-post-evaluation-applicator.mjs

      - name: Run patch applicator probe
        shell: cmd
        run: node scripts/apply-pr266-post-evaluation-patch.mjs
